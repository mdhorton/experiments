apply plugin: "java"

repositories {
    mavenCentral()
}

ext {
    mysqlVersion = "5.+"
    troveVersion = "3.+"
}

dependencies {
    compile "mysql:mysql-connector-java:$mysqlVersion",
            "net.sf.trove4j:trove4j:$troveVersion"
}
