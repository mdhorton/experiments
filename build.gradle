apply plugin: "java"

repositories {
    mavenCentral()
}

ext {
    logbackVersion = "1.+"
    troveVersion = "3.+"
}

dependencies {
    compile "ch.qos.logback:logback-classic:$logbackVersion",
            "net.sf.trove4j:trove4j:$troveVersion"
}
